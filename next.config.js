/** @type {import('next').NextConfig} */
const nextConfig = {
  // This file is just a pointer to help Vercel understand the project structure
  // The actual Next.js app is in the /ai directory
}

module.exports = nextConfig 